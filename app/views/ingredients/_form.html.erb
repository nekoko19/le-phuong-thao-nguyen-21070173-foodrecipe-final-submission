<%= form_with(model: ingredient) do |form| %>
<% if ingredient.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% ingredient.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :amount, class: label_class %>
          <%= form.number_field :amount, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :unit, class: label_class %>
          <%= form.text_field :unit, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if ingredient.persisted? %>
<%= link_to "Cancel", ingredient, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", ingredients_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
